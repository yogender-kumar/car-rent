import { CalcWidthPipe } from './calc-width.pipe';

describe('CalcWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
